# Negative Test — crm_probability_from_stage

**Level:** Negative — guard/keamanan, hal yang HARUS ditolak/tidak boleh muncul.

N/A — dikonfirmasi tidak ada skenario Negative aktif untuk modul ini (S-05 di `../10_BUSINESS_FLOW_MIGRATION.md`). Modul tidak punya wizard/`TransientModel` custom, tidak ada aksi yang memicu >1 dialog dari satu klik, dan tidak ada guard keamanan/permission khusus di luar yang bawaan Odoo (`base.group_user` untuk akses `crm.stage`/`crm.lead` standar).
